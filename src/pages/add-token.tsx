import React, { FC } from 'react';

// import { useGetUserInfoQuery } from '../store/auth-yard-api/auth-yard-api';

const AddTokenPage: FC = () => {
  // const { data: userInfo } = useGetUserInfoQuery();
  return <div>addToken:FC</div>;
};

export { AddTokenPage };
