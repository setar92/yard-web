import { createTheme } from '@mui/material';

export const theme = createTheme({
  palette: {
    primary: {
      light: '#ffffff',
      main: '#F28300',
      dark: '#0E0D16',
      contrastText: '#f1f4f6',
    },
  },
});
