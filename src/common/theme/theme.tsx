import { createTheme } from '@mui/material';

export const theme = createTheme({
  palette: {
    primary: {
      light: '#ffffff',
      main: '#F28300',
      dark: '#000000',
      contrastText: '#D3D3D3',
    },
    background: {
      default: '#f5f5f5',
    },
  },
});
